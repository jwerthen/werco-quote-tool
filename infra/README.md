# Infrastructure

This directory will contain Docker, Kubernetes, and Terraform assets.

## Planned Assets

- `docker-compose.yml` for local development.
- Container build configuration for backend and frontend.
- Production deployment manifests.
