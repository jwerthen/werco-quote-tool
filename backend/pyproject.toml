[project]
name = "quoteengine-backend"
version = "0.1.0"
description = "QuoteEngine FastAPI backend"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "pydantic>=2.6",
  "uvicorn>=0.27",
]

[tool.ruff]
line-length = 100

[tool.ruff.format]
quote-style = "double"
